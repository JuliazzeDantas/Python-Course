'''Exercise: Write a program to read two notes and print the arithmetic media between them.'''

note1 = float(input("Enter the student's first grade: "))

note2 = float(input("Enter the student's second grade: "))

print("The arithmetic avarage of the notes is {}". format((note1 + note2)/2))